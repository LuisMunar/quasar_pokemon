import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'quasar_pokemon',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
